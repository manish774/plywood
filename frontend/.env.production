# Loaded automatically by Vite for production builds (npm run build).
# Local dev keeps using .env (localhost) — this only affects deployed builds.

VITE_API_BASE_URL=https://plywood-backend-1ia4.onrender.com/api
VITE_USE_MOCK=false
